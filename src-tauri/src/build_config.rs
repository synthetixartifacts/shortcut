//! Build configuration — minimal stub for ShortCut OSS release.
//!
//! All configuration is runtime-only via `config.json` managed through the Settings UI.
//! No URL embedding. Production builds differ from dev only in optimization flags
//! and code signing.
