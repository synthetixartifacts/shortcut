/**
 * Dictation Constants
 *
 * Constants for dictation configuration.
 */

/** MediaRecorder chunk collection interval (ms) */
export const RECORDER_CHUNK_INTERVAL_MS = 100;

/** Minimum audio blob size to consider as containing speech (bytes) */
export const MIN_AUDIO_BLOB_SIZE = 1000;

/** Maximum length for error messages shown in indicator */
export const ERROR_TRUNCATION_LENGTH = 50;
